name: core
description: Pure foundation for the SDK framework. Backend-agnostic.
version: 0.0.1
homepage:
publish_to: none

environment:
  sdk: ^3.11.0
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
    
  localization: ^2.1.1
  flutter_localizations:
    sdk: flutter

  injector: ^4.0.0
  equatable: ^2.0.7
  flutter_bloc: ^9.1.1
  shared_preferences: ^2.5.3
  loggy: ^2.0.3
  bloc: ^9.0.0
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
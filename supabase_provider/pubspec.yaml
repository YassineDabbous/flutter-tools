name: supabase_provider
description: Supabase implementation for the logic skeleton using supabase_flutter.
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.11.0
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  skeleton:
    path: ../skeleton
  core:
    path: ../core
  
  supabase_flutter: ^2.8.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

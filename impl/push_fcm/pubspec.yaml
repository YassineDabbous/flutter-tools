name: push_fcm
description: FCM push bootstrap for Rebelo apps (mobile-only). Sole Firebase importer in the repo.
version: 0.0.1
homepage:
publish_to: none

environment:
  sdk: ^3.12.0

resolution: workspace

dependencies:
  flutter:
    sdk: flutter

  core: ^0.0.1
  notifications: ^0.0.1

  firebase_core: ^4.0.0
  firebase_messaging: ^16.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
